@model IEnumerable<CHBHTH.Models.NhaCungCap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Supplier</h2>

<p>
    @Html.ActionLink("Create Supplier", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNCC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNCC)
        </td>
        <td>
            <button class="btn btn-success">
                @Html.ActionLink("Edit", "Edit", new { id = item.MaNCC }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-primary">
                @Html.ActionLink("View", "Details", new { id = item.MaNCC }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-danger">
                @Html.ActionLink("Delete", "Delete", new { id = item.MaNCC }, new { @style = "color:white;" })
            </button>
        </td>
    </tr>
}

</table>
